# Active Review Control Room

Last checked: 2026-05-30 08:18 UTC

This page tracks public Spark Compete PRs that need fast, calm reviewer follow-up. It is intentionally about review readiness, not volume.

## Review SLA

- Run `python scripts/check_review_comments.py` before starting new work.
- Check active PRs before opening new work.
- If a reviewer comments, reply with either a fix, a clarification, or a clean close/adoption note.
- Keep each response short, specific, and tied to tests or packet validation.
- Do not add unsafe evidence, private maps, raw logs, account material, or unrelated scoring discussion.
- Do not force-push or broaden a PR while the user is away unless a maintainer explicitly asks for that exact action.

## Active PRs

| PR | Repo | Status | Packet | Verification | Next action |
| --- | --- | --- | --- | --- | --- |
| [#19](https://github.com/vibeforge1111/spark-voice-comms/pull/19) | `spark-voice-comms` | Open, maintainer follow-up asked for stable public-safe unsupported-runtime proof; branch pushed with follow-up commit `1f0ec5e`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Latest proof: `tests/test_spark_hook.py` -> 39 passed; `py_compile` passed; `git diff --check` passed; test asserts hostile path/secret/traceback text is omitted from the response | Await owner/security review. Latest proof reply: https://github.com/vibeforge1111/spark-voice-comms/pull/19#issuecomment-4577307198 |
| [#20](https://github.com/vibeforge1111/spark-voice-comms/pull/20) | `spark-voice-comms` | Open, maintainer follow-up asked for stable public-safe malformed JSON proof; branch pushed with follow-up commit `b47dc6c`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Latest proof: hook parse/error envelope tests -> 4 passed; `py_compile` passed; `git diff --check` passed; full suite still blocked by separate Python 3.14 Kokoro install lane covered by #19 and intentionally not mixed into this branch | Await owner/security review. Latest proof reply: https://github.com/vibeforge1111/spark-voice-comms/pull/20#issuecomment-4577307212 |
| [#21](https://github.com/vibeforge1111/spark-voice-comms/pull/21) | `spark-voice-comms` | Open, maintainer follow-up asked for stable public-safe malformed/oversized audio proof; branch pushed with follow-up commit `67db9f2`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Latest proof: audio/base64 and public error envelope tests -> 9 passed; `py_compile` passed; `git diff --check` passed; full suite still blocked by separate Python 3.14 Kokoro install lane covered by #19 and intentionally not mixed into this branch | Await owner/security review. Latest proof reply: https://github.com/vibeforge1111/spark-voice-comms/pull/21#issuecomment-4577307206 |
| [#23](https://github.com/vibeforge1111/spark-character/pull/23) | `spark-character` | Open, ready for review, mergeable clean, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Search adapter tests -> 14 passed; repo tests excluding external-chip baseline -> 88 passed, 9 deselected; redirect smoke confirmed three URL shapes | Watch for maintainer feedback. This is a focused live-search citation quality lane. |
| [#24](https://github.com/vibeforge1111/spark-character/pull/24) | `spark-character` | Open, latest maintainer security-safe redesign comment answered with prompt/LLM-boundary design note | Valid with owner-review warning: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | Search adapter tests -> 15 passed; broad non-trait-mutator suite -> 89 passed, 9 deselected; `py_compile` passed; full suite still blocked by known external `founder-operator` fixture lane | Await owner/security/lab review. Latest reply: https://github.com/vibeforge1111/spark-character/pull/24#issuecomment-4580522791 |
| [#27](https://github.com/vibeforge1111/spark-character/pull/27) | `spark-character` | Open, review-ledger/gate-review-pending owner comment received; no repair requested | Valid with owner-review warning: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | Focused limit guard tests -> 3 passed; `py_compile` passed; `git diff --check` passed; full suite reaches 95 passed and 3 existing local fixture failures in `tests/test_trait_mutator.py` because `founder-operator.personality.yaml` is absent | Await owner/security/lab review. This is separate from #23/#24 search behavior and #26 lowest-tier message actionability; it only makes zero/negative local eval-history limits return no rows instead of reading/scanning full local history. |
| [#17](https://github.com/vibeforge1111/spark-personality-chip-labs/pull/17) | `spark-personality-chip-labs` | Open, held for security-owner review before Mac Lab/points; branch pushed with follow-up commit `80a7134` to strengthen hook input-boundary proof; proof reply posted | Valid with owner-review warning after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning; draft packet at `/tmp/personality_pr17_packet_updated.json` | Spark hook tests -> 5 passed; full suite -> 256 passed; `py_compile` passed; `git diff --check` passed; missing-input error no longer includes the input path; oversized/non-object input rejected before handler execution | Await owner/security review. Proof reply: https://github.com/vibeforge1111/spark-personality-chip-labs/pull/17#issuecomment-4577192654 |
| [#18](https://github.com/vibeforge1111/spark-personality-chip-labs/pull/18) | `spark-personality-chip-labs` | Open, ready for review, mergeable clean, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Validator script tests -> 2 passed; full suite -> 255 passed; `py_compile` passed; empty-dir and packaged-dir smokes confirmed corrected exits | Watch for maintainer feedback. This is a focused personality validation accuracy lane. |
| [#20](https://github.com/vibeforge1111/spark-personality-chip-labs/pull/20) | `spark-personality-chip-labs` | Open, maintainer requested security-safe filesystem-boundary proof; proof reply posted; branch commit `2e93b53` | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Rechecked targeted proof: `PYTHONPATH=src python -m pytest -q tests/test_active.py` -> 13 passed; earlier full suite -> 254 passed; `py_compile` passed; `git diff --check` passed | Await owner/security/lab review. Reply explains no filesystem authority expansion, no unsafe evidence, and optional cache-write failure remains best-effort: https://github.com/vibeforge1111/spark-personality-chip-labs/pull/20#issuecomment-4578754867 |
| [#21](https://github.com/vibeforge1111/spark-personality-chip-labs/pull/21) | `spark-personality-chip-labs` | Open, maintainer requested security-safe filesystem-boundary proof; proof reply posted; branch commit `7eb3fde` | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Rechecked targeted proof: `PYTHONPATH=src python -m pytest -q tests/test_ib_connector.py` -> 18 passed; earlier full suite -> 255 passed; `py_compile` passed; `git diff --check` passed | Await owner/security/lab review. Reply explains no filesystem authority expansion, no unsafe evidence, and malformed local state recovery preserves valid counts: https://github.com/vibeforge1111/spark-personality-chip-labs/pull/21#issuecomment-4578757461 |
| [#22](https://github.com/vibeforge1111/spark-personality-chip-labs/pull/22) | `spark-personality-chip-labs` | Open; maintainer added `needs-security-redesign` and requested security-safe redesign/proof at https://github.com/vibeforge1111/spark-personality-chip-labs/pull/22#issuecomment-4578853639; proof reply posted | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Latest local recheck on Windows: malformed-input boundary tests -> 7 passed, 26 deselected; `py_compile` passed; `git diff --check` passed. Earlier branch proof: `tests/test_hooks.py` -> 33 passed; full suite -> 260 passed | Await security-owner/lab verification. Latest proof reply: https://github.com/vibeforge1111/spark-personality-chip-labs/pull/22#issuecomment-4579072582 |
| [#23](https://github.com/vibeforge1111/spark-personality-chip-labs/pull/23) | `spark-personality-chip-labs` | Open, gate-review comment answered; branch `codex/emotional-state-corrupt-file-guard` commit `6ec70d5`; non-object JSON state regression added; PR body validator section cleaned | Valid after refresh: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Before patch, corrupt valid-JSON `emotional_state.json` with malformed PAD/timestamp values raised `TypeError`, and top-level non-object JSON raised `AttributeError`; after patch `PYTHONPATH=src python -m pytest -q tests/test_emotional_state.py` -> 24 passed; full suite -> 255 passed; `py_compile` passed; `git diff --check` passed with line-ending warnings only | Await maintainer/security-owner review. Separate from PR #19 atomic/truncated-JSON recovery: this covers syntactically valid JSON with malformed typed fields or non-object decoded JSON. Gate note: https://github.com/vibeforge1111/spark-personality-chip-labs/pull/23#issuecomment-4579927928 |
| [#24](https://github.com/vibeforge1111/spark-domain-chip-labs/pull/24) | `spark-domain-chip-labs` | Open, ready for review, mergeable; GitHub shows unstable check state, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Targeted schema modules -> 6 skipped instead of collection errors; `PYTHONPATH=src python -m pytest -q` -> 1065 passed, 177 skipped, 1 unrelated saved-fixture mismatch; `py_compile` passed | Watch for maintainer feedback. This is a focused optional-dependency test collection lane; the saved-fixture mismatch is separate and outside this patch. |
| [#25](https://github.com/vibeforge1111/spark-domain-chip-labs/pull/25) | `spark-domain-chip-labs` | Open, ready for review, mergeable; GitHub shows unstable check state, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Exact saved-fixture test -> 1 passed; Startup YC operator-validation file -> 27 passed, 19 skipped; JSON validation passed; full suite still stops at unrelated #24 jsonschema blocker | Watch for maintainer feedback. This is a fixture-only provenance hash repair, split from #24. |
| [#28](https://github.com/vibeforge1111/spark-domain-chip-labs/pull/28) | `spark-domain-chip-labs` | Open, review-ledger/gate-review-pending owner comment received; no repair requested | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `PYTHONPATH=src python -m pytest -q tests/test_creator_mission_adapter.py` -> 10 passed, 2 skipped; `py_compile` passed; `git diff --check` passed | Await owner/security/lab review. This is separate from #24 test collection and #25 saved fixture hash: malformed or non-object creator mission packet input now exits with a bounded public-safe message instead of traceback/local path detail. |
| [#106](https://github.com/vibeforge1111/spark-intelligence-builder/pull/106) | `spark-intelligence-builder` | Open, latest maintainer security-safe redesign comment answered with attachment-hook trust-boundary design note | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Attachment hook suite -> 20 passed; `py_compile` passed; `git diff --check` passed | Await owner/security/lab review. Latest reply: https://github.com/vibeforge1111/spark-intelligence-builder/pull/106#issuecomment-4580522899 |
| [#112](https://github.com/vibeforge1111/spark-intelligence-builder/pull/112) | `spark-intelligence-builder` | Open, latest maintainer more-review-info comment answered with root issue, fix, proof, duplicate notes, and risk notes | Valid after body repair and recheck: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `tests/test_gateway_routes.py` -> 14 passed; gateway route + WhatsApp + Discord suites -> 48 passed; `py_compile` passed; `git diff --check` passed; broader Telegram gateway memory-delete tests fail independently and are not part of this patch | Await reviewer/lab. Patch narrows prefix routes to segment boundaries so `/api` no longer matches `/apiary` while intended descendants like `/api/task` still resolve. Latest reply: https://github.com/vibeforge1111/spark-intelligence-builder/pull/112#issuecomment-4580522871 |
| [#56](https://github.com/vibeforge1111/Spark-Agent-Site/pull/56) | `Spark-Agent-Site` | Open, ready for review, mergeable clean, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Docs/readiness checks passed before PR; body routes Memory chip feedback from `domain-chip-memory` to `spark-domain-chip-labs` and keeps the change docs-only | Watch for maintainer feedback. This is a focused feedback-route accuracy lane. |
| [#63](https://github.com/vibeforge1111/Spark-Agent-Site/pull/63) | `Spark-Agent-Site` | Open; maintainer-adoption follow-up says targeted tests/proof are still missing at https://github.com/vibeforge1111/Spark-Agent-Site/pull/63#issuecomment-4578928426; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Rechecked on Windows: `node scripts/check-security-release-surface.mjs` -> passed; `node scripts/check-docs-readiness.mjs` -> passed; `node scripts/check-command-docs.mjs` -> passed; `rg` stale-route smoke -> only the checker guard remains | Await maintainer/security-owner/lab verification. Latest proof reply: https://github.com/vibeforge1111/Spark-Agent-Site/pull/63#issuecomment-4579072476 |
| [#65](https://github.com/vibeforge1111/Spark-Agent-Site/pull/65) | `Spark-Agent-Site` | Open, maintainer requested release/metadata verification; proof reply posted | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `node scripts/check-docs-readiness.mjs` -> passed; `node scripts/check-security-release-surface.mjs` -> passed; `node scripts/check-command-docs.mjs` -> passed; `git diff --check` passed; proof points to rendered updates page, overview CTA, `llms.txt`, and readiness assertions all agreeing on May 29 r21 | Await release/metadata review. Proof reply: https://github.com/vibeforge1111/Spark-Agent-Site/pull/65#issuecomment-4577423658 |
| [#133](https://github.com/vibeforge1111/vibeship-spawner-ui/pull/133) | `vibeship-spawner-ui` | Open; security-owner hold requested exact trust-boundary/risk proof; latest proof reply posted | Valid with owner-review warning after proof refresh: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning; draft packet at `/tmp/spawner_pr133_packet_updated.json` | Rechecked lockfile boundary: `npm audit --audit-level=moderate` -> exits clean for moderate/high with only low cookie advisory remaining; `git diff --check` -> passed; earlier `npm test -- --run` -> 99 files / 649 tests passed, `npm run check` -> 0 errors/warnings, `npm run build` -> passed | Await security-owner/lab verification. Latest reply states package-lock-only dependency provenance boundary, no source/auth/runtime/CI/file/network/prompt changes, and lockfile-only rollback: https://github.com/vibeforge1111/vibeship-spawner-ui/pull/133#issuecomment-4579525781 |
| [#137](https://github.com/vibeforge1111/vibeship-spawner-ui/pull/137) | `vibeship-spawner-ui` | Open, latest maintainer security-safe redesign comment answered with self-start smoke-script boundary note | Valid after body repair: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `node --check scripts/smoke-mission-surfaces.mjs` -> passed; `git diff --check` -> passed; targeted Vitest scripts -> 17 passed; `SPAWNER_MISSION_SURFACES_SMOKE_PORT=5574 npm run smoke:mission-surfaces` -> 8 PASS checks; `npm run check` -> 0 errors/warnings; full Vitest suite passed before script-only edit with 99 files / 649 tests | Await owner/security/lab review. Latest reply: https://github.com/vibeforge1111/vibeship-spawner-ui/pull/137#issuecomment-4580522869 |
| [#138](https://github.com/vibeforge1111/vibeship-spawner-ui/pull/138) | `vibeship-spawner-ui` | Open, ready for review, mergeable; branch `codex/storage-helpers-unavailable-guard` commit `e4d9fed`; no comments yet | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Focused storage helper tests -> 3 passed; `npm run check` -> 0 errors/warnings; full Vitest suite -> 100 files / 652 tests passed; `npm run build` -> passed; `git diff --check` -> passed | Watch for maintainer feedback. Patch guards shared raw browser storage helpers when `localStorage` is blocked/unavailable, separate from #133 lockfile audit and #137 mission-surface smoke self-start. |
| [#139](https://github.com/vibeforge1111/vibeship-spawner-ui/pull/139) | `vibeship-spawner-ui` | Open, maintainer asked for public-safe active mission state/API proof; proof reply posted; PR body formatting refreshed again to remove stale `$rest` artifact; branch `codex/active-mission-corrupt-state-guard` commit `98a5d03` | Valid after formatting refresh: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Rechecked focused active mission tests -> 7 passed; `git diff --check` -> passed; earlier `npm run check` -> 0 errors/warnings, full Vitest suite -> 99 files / 650 tests passed, `npm run build` -> passed | Await security-owner/lab verification. Reply explains corrupt `active-mission.json` now returns bounded inactive/corrupt JSON without mission payload fields, `executionPrompt`, tasks, private paths, secrets, raw logs, stack traces, or internal maps: https://github.com/vibeforge1111/vibeship-spawner-ui/pull/139#issuecomment-4579486062 |
| [#50](https://github.com/vibeforge1111/spark-researcher/pull/50) | `spark-researcher` | Open, held for security-owner review before Mac Lab/points because this is a research advisory-boundary surface; proof reply posted | Valid after safe packet rewording: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings; draft packet at `/tmp/researcher_pr50_packet_updated.json` | Prompt-boundary tests -> 9 passed; full suite -> 80 passed; `py_compile` passed; before/after smoke confirms unsafe source URLs are omitted | Await owner/security review. Proof reply: https://github.com/vibeforge1111/spark-researcher/pull/50#issuecomment-4577193073 |
| [#52](https://github.com/vibeforge1111/spark-researcher/pull/52) | `spark-researcher` | Open, maintainer requested public-safe missing-config proof; branch pushed with follow-up commit `f4982dd`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Missing-config tests -> 8 passed; full suite -> 85 passed; `py_compile` passed; `git diff --check` passed; external config paths collapse to `<external-config>` and hostile private path/secret-shaped filename is not echoed | Await owner/security review. Proof reply: https://github.com/vibeforge1111/spark-researcher/pull/52#issuecomment-4577423687 |
| [#53](https://github.com/vibeforge1111/spark-researcher/pull/53) | `spark-researcher` | Open, mergeable; maintainer opened trusted adoption PR [#54](https://github.com/vibeforge1111/spark-researcher/pull/54) for the minimal safe behavior; adoption acknowledgement posted; participant branch commit `46d2687` unchanged | Valid with owner-review warning after body repair: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | Original participant proof: chip contract tests -> 9 passed; full suite -> 78 passed; `py_compile` passed; `git diff --check` passed. Trusted adoption PR #54 body reports focused chip-hook tests -> 5 passed, full chip contracts -> 9 passed, full suite -> 78 passed, `compileall` passed, and `git diff --check` passed | Await #54 maintainer/security/lab outcome. Do not broaden or force-push #53 unless reviewer asks. Latest acknowledgement: https://github.com/vibeforge1111/spark-researcher/pull/53#issuecomment-4579254451 |
| [#441](https://github.com/vibeforge1111/spark-cli/pull/441) | `spark-cli` | Open, ready for review, mergeable; GitHub shows blocked merge state | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Update repair tests -> 3 passed; `py_compile` passed; empty-home and installed-home smokes confirmed routing | Watch for maintainer feedback. This is a clean repair-guidance lane with no validator warnings. |
| [#451](https://github.com/vibeforge1111/spark-cli/pull/451) | `spark-cli` | Open, ready for owner/security review, mergeable clean, no comments; branch pushed with follow-up commit `b8e88c2` and PR body updated after GitHub auth was restored | Valid with owner-review warning on updated packet: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | Bundle-aware fix/verify tests -> 4 passed; broader repair/verify lane -> 19 passed; `py_compile` passed; branch-local `fix telegram --json` and `verify --onboarding --json` smokes show `telegram-voice-starter` repair commands | Await owner/security review. This extends the existing bundle-aware repair-output lane to `spark fix telegram` instead of opening a duplicate PR. |
| [#461](https://github.com/vibeforge1111/spark-cli/pull/461) | `spark-cli` | Open, clean replacement for #452; one commit `5c6a5ae` from current `origin/master`; maintainer gate-review comment answered; PR body validator section cleaned | Valid with owner-review warning: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | `PYTHONPATH=src python tests/test_browser_use_cli.py` -> 23 passed; `py_compile` passed; `git diff --check origin/master..HEAD` passed | Await owner/security/lab review. Replaces #452 after maintainer requested focused rebase/replacement; same root issue and files only. Gate note: https://github.com/vibeforge1111/spark-cli/pull/461#issuecomment-4579909902 |
| [#462](https://github.com/vibeforge1111/spark-cli/pull/462) | `spark-cli` | Open, review-ledger/gate-review-pending owner comment received; no repair requested | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `PYTHONPATH=src python tests/test_browser_use_cli.py` -> 22 passed; `py_compile` passed; `git diff --check origin/master..HEAD` passed | Await owner/security/lab review. Replaces #453 after maintainer requested focused rebase/replacement; same root issue and files only. |
| [#440](https://github.com/vibeforge1111/spark-cli/pull/440) | `spark-cli` | Open, latest maintainer security-safe redesign comment answered with CLI execution-shaped output-boundary design note | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Summary tests -> 7 passed, 567 deselected; broader summary/fix/healthcheck lane -> 8 passed, 566 deselected; `py_compile` passed; `git diff --check` passed | Await owner/security/lab review. Latest reply: https://github.com/vibeforge1111/spark-cli/pull/440#issuecomment-4580522880 |
| [#439](https://github.com/vibeforge1111/spark-cli/pull/439) | `spark-cli` | Open, ready for review, mergeable; GitHub shows blocked merge state | Valid: `packet_valid=true`, `can_continue_to_review=true`; validator warning `security_owner_review_expected` for sign-in readiness gate | Targeted provider-readiness tests -> 10 passed; `py_compile` passed; isolated onboarding smoke shows after-fix `llm_roles` failure | Watch for security/lab or maintainer feedback. Keep responses focused on redaction, sign-in readiness, and reproducible smoke. |
| [#438](https://github.com/vibeforge1111/spark-cli/pull/438) | `spark-cli` | Open, mergeable; Mac Lab failure comment answered with test-only fixture repair; GitHub now reports branch behind | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Focused live/external-ingress tests -> 5 passed after repair; previous full `tests/test_cli.py` -> 572 passed, 1 skipped | Await lab rerun/reviewer response. Do not update branch unless the reviewer asks or the behind state becomes the blocker. |
| [#278](https://github.com/vibeforge1111/spark-telegram-bot/pull/278) | `spark-telegram-bot` | Open, ready for review, mergeable clean; Telegram proof hold asked for disposable-chat smoke path or clear no-safe-chat note; proof reply posted | Valid with expected Telegram no-safe-chat warning: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `telegram_proof_unavailable` warning | Rechecked direct Builder bridge tests -> passed; `npm run build` -> passed; `git diff --check` -> passed; repo runner previously continued into unrelated buildE2E self-awareness failure; npm audit reports existing axios lane | Await reviewer/lab. Reply states no safe disposable Telegram chat is available, gives maintainer/lab smoke shape, and keeps proof bounded to parser-level receipt freshness: https://github.com/vibeforge1111/spark-telegram-bot/pull/278#issuecomment-4579525898 |
| [#279](https://github.com/vibeforge1111/spark-telegram-bot/pull/279) | `spark-telegram-bot` | Open, ready for review, mergeable clean; Telegram proof hold asked for disposable-chat smoke path or clear no-safe-chat note; proof reply posted | Valid with expected Telegram no-safe-chat warning: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `telegram_proof_unavailable` warning | Rechecked `BOT_TOKEN=123:test node node_modules/ts-node/dist/bin.js tests/spawnerLoopBugHunt.test.ts` -> passed; `npm run build` -> passed; `git diff --check` -> passed; regression fills a user window, blocks a fourth normal/action command, and proves `/myid` plus `/start session-123` still pass | Await reviewer/lab. Reply states no safe disposable Telegram chat is available and gives live disposable smoke shape: saturate burst window, confirm `/myid` and `/start session-123` still respond while `/run mission` remains rate-limited: https://github.com/vibeforge1111/spark-telegram-bot/pull/279#issuecomment-4579525987 |
| [#282](https://github.com/vibeforge1111/spark-telegram-bot/pull/282) | `spark-telegram-bot` | Open, Telegram proof hold asked for exact no-safe-chat wording and smoke path; proof reply posted; malformed packet fence and validator/test formatting repaired; branch `codex/spawner-board-error-redaction` commit `f518fb9` | Valid with expected warnings after formatting refresh: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 2 warnings (`telegram_proof_unavailable`, `security_owner_review_expected`) | Before patch focused Spawner tests failed because direct `/board` and natural board summary paths returned raw Mission Control detail with a private path and secret-shaped token; after follow-up `node node_modules/ts-node/dist/bin.js tests/spawner.test.ts` -> passed; `npm run build` -> passed; `git diff --check` -> passed | Await security-owner/lab verification. Latest reply uses exact no-safe-chat limitation wording, points to mocked command-flow proof, and gives maintainer/lab disposable smoke path: https://github.com/vibeforge1111/spark-telegram-bot/pull/282#issuecomment-4579654510 |
| [#224](https://github.com/vibeforge1111/spark-telegram-bot/pull/224) | `spark-telegram-bot` | Open; maintainer requested reshape/split; reply posted | Older packet status not used as next lane | Maintainer/lab previously reported Mac Lab passed; upstream now has patch-equivalent commit `014f17f` | Do not force-push unless reviewer asks. Treat as adopted/overtaken and keep the public note concise. |

## Closed Or Overtaken PRs

| PR | Repo | Result | Evidence | Follow-up |
| --- | --- | --- | --- | --- |
| [#419](https://github.com/vibeforge1111/spark-cli/pull/419) | `spark-cli` | Closed as overtaken/adopted after maintainer asked for a rebase and current-pin reduction | Current `origin/master` at `b07d72d` already carries the current registry pins after merged registry path #421 (`f313910`); `verify --registry-pins --json` and `verify --provenance --json` both returned `ok: true` on a clean current-master checkout | Close comment posted: https://github.com/vibeforge1111/spark-cli/pull/419#issuecomment-4577503586 |
| [#452](https://github.com/vibeforge1111/spark-cli/pull/452) | `spark-cli` | Closed after maintainer requested focused rebase/replacement | Clean replacement #461 is one focused commit from current `origin/master`; replacement packet validated with 0 errors and 1 expected owner-review warning | Close/replacement comment posted: https://github.com/vibeforge1111/spark-cli/pull/452#issuecomment-4579758647 |
| [#453](https://github.com/vibeforge1111/spark-cli/pull/453) | `spark-cli` | Closed after maintainer requested focused rebase/replacement | Clean replacement #462 is one focused commit from current `origin/master`; replacement packet validated with 0 errors/warnings | Close/replacement comment posted: https://github.com/vibeforge1111/spark-cli/pull/453#issuecomment-4579758947 |

## Ready Replies

## Posted Review Replies

### Spark-Agent-Site #63

Thanks, rechecked the targeted public-safe proof for the legal-footer route surface.

What changed remains scoped to `privacy.html`, `terms.html`, `cookies.html`, and `scripts/check-security-release-surface.mjs`. The checker now guards the exact public release files so the legal pages cannot regress to the retired `spark-skill-graphs` GitHub route.

Safe proof:
- `node scripts/check-security-release-surface.mjs` -> passed
- `node scripts/check-docs-readiness.mjs` -> passed
- `node scripts/check-command-docs.mjs` -> passed
- `rg -n "github.com/vibeforge1111/spark-skill-graphs|spark-skill-graphs" privacy.html terms.html cookies.html index.html docs llms.txt llms-full.txt README.md scripts/check-security-release-surface.mjs` -> only the checker guard remains

Still needs maintainer/security-owner/lab verification; validator/test proof does not waive those gates.

### spark-personality-chip-labs #22

Thanks, rechecked this as a minimal hook input-boundary repair.

What changed remains scoped to `src/personality_engine/hooks.py` and `tests/test_hooks.py`. The hook keeps valid dict-shaped `tool_input` behavior unchanged, but non-object `tool_input` is treated as unusable input and skipped before command inspection. That does not add dependencies, CI, auth, network access, filesystem access, prompt-boundary expansion, or raw payload echo.

Safe proof:
- `PYTHONPATH=src python -m pytest -q tests/test_hooks.py -k malformed` -> 7 passed, 26 deselected
- `python -m py_compile src/personality_engine/hooks.py tests/test_hooks.py` -> passed
- `git diff --check` -> passed

Still needs security-owner/lab verification; validator/test proof does not waive those gates.

### vibeship-spawner-ui #133

Thanks, rechecked this as a lockfile-only dependency/release-boundary repair and kept the branch scoped to `package-lock.json`.

Focused smoke path for maintainer/lab:
- start from the PR branch
- run `npm ci`
- run `npm audit --audit-level=moderate`
- run `npm test -- --run`
- run `npm run check`
- run `npm run build`

Safe proof from the current branch:
- `npm audit --audit-level=moderate` -> exits clean; only the upstream low `cookie` advisory remains, and `npm audit fix --force` would force a breaking SvelteKit downgrade path
- `npm test -- --run` -> 99 files / 649 tests passed
- `npm run check` -> 0 errors / 0 warnings
- `npm run build` -> passed

Boundary note: the diff is package-lock-only. It does not change app source, auth, API routes, runtime/container behavior, network behavior, filesystem behavior, installer metadata, CI, scripts, deletion surfaces, or prompt/LLM paths. The one trust boundary is dependency/release provenance through the npm lockfile; high/moderate audit findings are removed without widening application authority, while the remaining low advisory is left for upstream SvelteKit instead of forcing a breaking downgrade.

Still needs maintainer/security-owner/lab verification; validator/test proof does not waive those gates.

### Packet Or Format Issue

Thanks, fixed. I repaired the packet in the PR body and re-ran the Spark Compete validator. Current result: `packet_valid=true`, `can_continue_to_review=true`, with 0 errors and 0 warnings.

### Tests Or Repro Question

Thanks, confirmed. The before state reproduces with the command listed in the packet, and the current branch verifies with the listed test command. I kept the patch scoped to the files named in the packet.

### Split Or Scope Concern

Thanks, agreed. I will keep this to one root cause and one owner surface. If this PR is too broad, I can close it and open a smaller focused replacement rather than force-pushing unrelated changes.

### Overtaken Or Duplicate

Thanks, confirmed. I checked current upstream before changing the branch. If the fix is already patch-equivalent upstream, I will not force-push noise; please treat this as adopted/overtaken or tell me if you prefer a clean replacement PR.

## Next Work Gate

Open another PR only when all are true:

- It is in an allowed public repo.
- The issue is reproducible without private evidence.
- The patch is one root cause and one owner surface.
- Tests pass locally.
- The PR body includes a validator-clean `spark-compete-hotfix-v1` packet.
- Existing review comments are already answered.

## Candidate Queue

| Candidate | Repo | Why it may be worth checking | Gate before work |
| --- | --- | --- | --- |
| Research URL/evidence handling | `spark-researcher` | PR #50 covers unsafe/non-public DuckDuckGo result URLs being preserved as research prompt sources. Researcher is also crowded around adjacent surfaces: #52 covers missing-config guidance, #53/#54 cover chip-hook public error redaction/adoption, and open upstream PRs cover safe URL, subprocess timeout, and CLI validation lanes. | Avoid duplicating citation URL hygiene, missing-config guidance, chip-hook redaction, safe-URL guardrails, subprocess timeouts, or generic CLI validation unless a reviewer asks or a clearly separate root cause appears. |
| Agent-site legal footer route | `Spark-Agent-Site` | PR #63 covers stale legal-page GitHub links to retired `spark-skill-graphs`. Agent Site is also crowded around adjacent surfaces: #56 covers memory feedback routing, #65 covers r21 release metadata, #66/#67 cover readiness/checker messaging, and open upstream PRs already cover installer/docs safety and several XSS/innerHTML lanes. | Avoid duplicating legal/footer routes, memory feedback routing, current-release labels, readiness checker wording, installer/docs safety, or XSS/innerHTML fixes unless a reviewer asks or a clearly separate root cause appears. |
| Spawner UI dependency/runtime baseline | `vibeship-spawner-ui` | PR #133 covers the fresh-install high/moderate npm audit baseline with a lockfile-only compatible refresh. Spawner UI Mission Control/provider-result areas are crowded: provider result artifacts already have #92, state isolation has #86/#87, orphan start handling has #77, and JUMPERZ already has #137/#138/#139 open. | Avoid dependency-audit duplicates and avoid provider-result/Mission-Control duplicate lanes unless a reviewer asks or a clearly separate, reproducible root cause appears. |
| Spark CLI onboarding verify guidance | `spark-cli` | PR #451 covers configured-bundle repair commands for `spark verify --onboarding`; #438-#441 cover separate CLI repair/readiness lanes. | Avoid more CLI guidance PRs unless the failure is clearly separate from bundle-aware verify output, repair summaries, provider readiness, live ingress, or update repair. |
| Voice install/onboard edge cases | `spark-voice-comms` | PR #19 covers Kokoro Python support, PR #20 covers invalid hook JSON shape, and PR #21 covers malformed `voice.transcribe` audio base64. | Avoid those three lanes; inspect a separate runtime/provider or docs path only if it has a distinct root cause. |
| Telegram rate-limit user notice | `spark-telegram-bot` | Generic notice behavior already has open PR #137; #279 now covers the separate `/start` and `/myid` onboarding reachability gap. | Do not open more rate-limit lanes unless reviewer asks or a distinct non-overlapping root cause appears. |
| Builder attachment hook execution | `spark-intelligence-builder` | PR #106 covers process-0/output-returncode-1 misclassification. | Avoid duplicating hook returncode; inspect other builder surfaces only if separate root cause. |
| Character eval/history local tooling | `spark-character` | PR #27 covers zero/negative eval-history limits reading or scanning local rows. PR #23 covers DuckDuckGo redirect URL decoding; PR #24 covers live-search snippet prompt-boundary sanitization. | Avoid duplicating those lanes; inspect another character surface only if it has a separate root cause. |
| Personality active cache hardening | `spark-personality-chip-labs` | PR #20 now covers blocked cache-parent failures during valid active personality resolution. | Do not open more active-cache lanes unless reviewer asks or a distinct non-overlapping root cause appears. |
| Personality evolver state recovery | `spark-personality-chip-labs` | PR #21 now covers malformed existing `personality_evolution_v1.json` interaction-count/non-object recovery. | Do not open more evolver-state recovery lanes unless reviewer asks or a distinct non-overlapping root cause appears. |
| Personality hook host-output contract | `spark-personality-chip-labs` | PR #17 covers missing/non-object input in `spark_hook.py`; adjacent hook boundaries may still be worth checking. | Avoid overlapping PR #13 unsupported hook and PR #16 `hooks.py` stdin handling. |
| Personality validator CLI | `spark-personality-chip-labs` | PR #18 covers empty directories, template skipping, and current bridge summary keys. | Avoid overlapping directory validation behavior; look elsewhere unless reviewers ask. |
| Domain-chip local test collection and product-surface adapter | `spark-domain-chip-labs` | PR #24 covers optional `jsonschema` collection failures; PR #25 covers the Startup YC saved fixture hash mismatch; PR #28 covers creator-mission-status malformed packet load errors. | Avoid duplicating importorskip, saved-hash, or creator-mission packet-load work; move to another domain-chip lane only if it has a separate root cause. |

## Do Not Pursue

| Candidate | Repo | Reason |
| --- | --- | --- |
| Packet 089 / local `spark-cli-hunt` config null round-trip | `spark-cli` | Do not open from this workspace or mix into PR [#438](https://github.com/vibeforge1111/spark-cli/pull/438). Upstream PR [#467](https://github.com/vibeforge1111/spark-cli/pull/467) already covers the same `spark config get` explicit JSON `null` issue. |
