# Active Review Control Room

Last checked: 2026-05-29 18:18 UTC

This page tracks public Spark Compete PRs that need fast, calm reviewer follow-up. It is intentionally about review readiness, not volume.

## Review SLA

- Run `python scripts/check_review_comments.py` before starting new work.
- Check active PRs before opening new work.
- If a reviewer comments, reply with either a fix, a clarification, or a clean close/adoption note.
- Keep each response short, specific, and tied to tests or packet validation.
- Do not add unsafe evidence, private maps, raw logs, account material, or unrelated scoring discussion.
- Do not force-push or broaden a PR while the user is away unless a maintainer explicitly asks for that exact action.

## Active PRs

| PR | Repo | Status | Packet | Verification | Next action |
| --- | --- | --- | --- | --- | --- |
| [#19](https://github.com/vibeforge1111/spark-voice-comms/pull/19) | `spark-voice-comms` | Open, maintainer follow-up asked for stable public-safe unsupported-runtime proof; branch pushed with follow-up commit `1f0ec5e`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Latest proof: `tests/test_spark_hook.py` -> 39 passed; `py_compile` passed; `git diff --check` passed; test asserts hostile path/secret/traceback text is omitted from the response | Await owner/security review. Latest proof reply: https://github.com/vibeforge1111/spark-voice-comms/pull/19#issuecomment-4577307198 |
| [#20](https://github.com/vibeforge1111/spark-voice-comms/pull/20) | `spark-voice-comms` | Open, maintainer follow-up asked for stable public-safe malformed JSON proof; branch pushed with follow-up commit `b47dc6c`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Latest proof: hook parse/error envelope tests -> 4 passed; `py_compile` passed; `git diff --check` passed; full suite still blocked by separate Python 3.14 Kokoro install lane covered by #19 and intentionally not mixed into this branch | Await owner/security review. Latest proof reply: https://github.com/vibeforge1111/spark-voice-comms/pull/20#issuecomment-4577307212 |
| [#21](https://github.com/vibeforge1111/spark-voice-comms/pull/21) | `spark-voice-comms` | Open, maintainer follow-up asked for stable public-safe malformed/oversized audio proof; branch pushed with follow-up commit `67db9f2`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Latest proof: audio/base64 and public error envelope tests -> 9 passed; `py_compile` passed; `git diff --check` passed; full suite still blocked by separate Python 3.14 Kokoro install lane covered by #19 and intentionally not mixed into this branch | Await owner/security review. Latest proof reply: https://github.com/vibeforge1111/spark-voice-comms/pull/21#issuecomment-4577307206 |
| [#23](https://github.com/vibeforge1111/spark-character/pull/23) | `spark-character` | Open, ready for review, mergeable clean, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Search adapter tests -> 14 passed; repo tests excluding external-chip baseline -> 88 passed, 9 deselected; redirect smoke confirmed three URL shapes | Watch for maintainer feedback. This is a focused live-search citation quality lane. |
| [#24](https://github.com/vibeforge1111/spark-character/pull/24) | `spark-character` | Open, ready for owner/security review, mergeable clean; maintainer requested minimal prompt/LLM-boundary hardening and reply posted | Valid with owner-review warning: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | Search adapter tests -> 15 passed; broad non-trait-mutator suite -> 89 passed, 9 deselected; `py_compile` passed; full suite still blocked by known external `founder-operator` fixture lane | Await owner/security review before Mac Lab. This now has adversarial snippet tests proving untrusted search text stays data, not instructions; no dependencies, CI, auth, installer, network backend, provider behavior, or unrelated runtime changes. |
| [#17](https://github.com/vibeforge1111/spark-personality-chip-labs/pull/17) | `spark-personality-chip-labs` | Open, held for security-owner review before Mac Lab/points; branch pushed with follow-up commit `80a7134` to strengthen hook input-boundary proof; proof reply posted | Valid with owner-review warning after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning; draft packet at `/tmp/personality_pr17_packet_updated.json` | Spark hook tests -> 5 passed; full suite -> 256 passed; `py_compile` passed; `git diff --check` passed; missing-input error no longer includes the input path; oversized/non-object input rejected before handler execution | Await owner/security review. Proof reply: https://github.com/vibeforge1111/spark-personality-chip-labs/pull/17#issuecomment-4577192654 |
| [#18](https://github.com/vibeforge1111/spark-personality-chip-labs/pull/18) | `spark-personality-chip-labs` | Open, ready for review, mergeable clean, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Validator script tests -> 2 passed; full suite -> 255 passed; `py_compile` passed; empty-dir and packaged-dir smokes confirmed corrected exits | Watch for maintainer feedback. This is a focused personality validation accuracy lane. |
| [#24](https://github.com/vibeforge1111/spark-domain-chip-labs/pull/24) | `spark-domain-chip-labs` | Open, ready for review, mergeable; GitHub shows unstable check state, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Targeted schema modules -> 6 skipped instead of collection errors; `PYTHONPATH=src python -m pytest -q` -> 1065 passed, 177 skipped, 1 unrelated saved-fixture mismatch; `py_compile` passed | Watch for maintainer feedback. This is a focused optional-dependency test collection lane; the saved-fixture mismatch is separate and outside this patch. |
| [#25](https://github.com/vibeforge1111/spark-domain-chip-labs/pull/25) | `spark-domain-chip-labs` | Open, ready for review, mergeable; GitHub shows unstable check state, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Exact saved-fixture test -> 1 passed; Startup YC operator-validation file -> 27 passed, 19 skipped; JSON validation passed; full suite still stops at unrelated #24 jsonschema blocker | Watch for maintainer feedback. This is a fixture-only provenance hash repair, split from #24. |
| [#106](https://github.com/vibeforge1111/spark-intelligence-builder/pull/106) | `spark-intelligence-builder` | Open, maintainer requested minimal attachment-hook trust-boundary proof; branch pushed with follow-up commit `a522b40`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Attachment hook suite -> 20 passed; `py_compile` passed; `git diff --check` passed | Await owner/security review. Proof reply: https://github.com/vibeforge1111/spark-intelligence-builder/pull/106#issuecomment-4577190197 |
| [#112](https://github.com/vibeforge1111/spark-intelligence-builder/pull/112) | `spark-intelligence-builder` | Open, ready for review, mergeable clean after maintainer invalid-packet comment was repaired; branch commit `93c7242`; latest gate-focus proof reply posted | Valid after body repair and recheck: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `tests/test_gateway_routes.py` -> 14 passed; gateway route + WhatsApp + Discord suites -> 48 passed; `py_compile` passed; `git diff --check` passed; broader Telegram gateway memory-delete tests fail independently and are not part of this patch | Await reviewer/lab. Patch narrows prefix routes to segment boundaries so `/api` no longer matches `/apiary` while intended descendants like `/api/task` still resolve. Latest reply: https://github.com/vibeforge1111/spark-intelligence-builder/pull/112#issuecomment-4578218209 |
| [#56](https://github.com/vibeforge1111/Spark-Agent-Site/pull/56) | `Spark-Agent-Site` | Open, ready for review, mergeable clean, no comments | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Docs/readiness checks passed before PR; body routes Memory chip feedback from `domain-chip-memory` to `spark-domain-chip-labs` and keeps the change docs-only | Watch for maintainer feedback. This is a focused feedback-route accuracy lane. |
| [#63](https://github.com/vibeforge1111/Spark-Agent-Site/pull/63) | `Spark-Agent-Site` | Open, maintainer requested minimal release-surface proof; branch pushed with follow-up commit `1578975`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `node scripts/check-security-release-surface.mjs` -> passed; docs readiness -> passed; command docs -> passed; branch diff scope -> `cookies.html`, `privacy.html`, `scripts/check-security-release-surface.mjs`, `terms.html` | Await owner/security review. Proof reply: https://github.com/vibeforge1111/Spark-Agent-Site/pull/63#issuecomment-4577190419 |
| [#65](https://github.com/vibeforge1111/Spark-Agent-Site/pull/65) | `Spark-Agent-Site` | Open, maintainer requested release/metadata verification; proof reply posted | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `node scripts/check-docs-readiness.mjs` -> passed; `node scripts/check-security-release-surface.mjs` -> passed; `node scripts/check-command-docs.mjs` -> passed; `git diff --check` passed; proof points to rendered updates page, overview CTA, `llms.txt`, and readiness assertions all agreeing on May 29 r21 | Await release/metadata review. Proof reply: https://github.com/vibeforge1111/Spark-Agent-Site/pull/65#issuecomment-4577423658 |
| [#133](https://github.com/vibeforge1111/vibeship-spawner-ui/pull/133) | `vibeship-spawner-ui` | Open, held for security-owner review before Mac Lab/points because this is a dependency lockfile/security surface; proof reply posted | Valid with owner-review warning after proof refresh: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning; draft packet at `/tmp/spawner_pr133_packet_updated.json` | `npm audit --audit-level=moderate` -> exits clean with only low cookie advisory remaining; `npm test -- --run` -> 99 files / 649 tests passed; `npm run check` -> 0 errors/warnings; `npm run build` -> passed | Await owner/security review. Proof reply: https://github.com/vibeforge1111/vibeship-spawner-ui/pull/133#issuecomment-4577192877 |
| [#137](https://github.com/vibeforge1111/vibeship-spawner-ui/pull/137) | `vibeship-spawner-ui` | Open, packet shape accepted; maintainer requested explicit self-start safety proof and proof reply posted; branch commit `cb7e60d` unchanged | Valid after body repair: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `node --check scripts/smoke-mission-surfaces.mjs` -> passed; `git diff --check` -> passed; targeted Vitest scripts -> 17 passed; `SPAWNER_MISSION_SURFACES_SMOKE_PORT=5574 npm run smoke:mission-surfaces` -> 8 PASS checks; `npm run check` -> 0 errors/warnings; full Vitest suite passed before script-only edit with 99 files / 649 tests | Await owner/security review. Safety proof says only local Vite starts on `127.0.0.1`, smoke calls stay on the base URL, `autoRun:false` prevents provider dispatch, failures are bounded and child shutdown happens in `finally`. Proof reply: https://github.com/vibeforge1111/vibeship-spawner-ui/pull/137#issuecomment-4577877842 |
| [#50](https://github.com/vibeforge1111/spark-researcher/pull/50) | `spark-researcher` | Open, held for security-owner review before Mac Lab/points because this is a research advisory-boundary surface; proof reply posted | Valid after safe packet rewording: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings; draft packet at `/tmp/researcher_pr50_packet_updated.json` | Prompt-boundary tests -> 9 passed; full suite -> 80 passed; `py_compile` passed; before/after smoke confirms unsafe source URLs are omitted | Await owner/security review. Proof reply: https://github.com/vibeforge1111/spark-researcher/pull/50#issuecomment-4577193073 |
| [#52](https://github.com/vibeforge1111/spark-researcher/pull/52) | `spark-researcher` | Open, maintainer requested public-safe missing-config proof; branch pushed with follow-up commit `f4982dd`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Missing-config tests -> 8 passed; full suite -> 85 passed; `py_compile` passed; `git diff --check` passed; external config paths collapse to `<external-config>` and hostile private path/secret-shaped filename is not echoed | Await owner/security review. Proof reply: https://github.com/vibeforge1111/spark-researcher/pull/52#issuecomment-4577423687 |
| [#53](https://github.com/vibeforge1111/spark-researcher/pull/53) | `spark-researcher` | Open, mergeable; maintainer saw draft placeholder body, then body was repaired with complete packet and proof reply was posted; branch commit `46d2687` unchanged | Valid with owner-review warning after body repair: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | Chip contract tests -> 9 passed; full suite -> 78 passed; `py_compile` passed; `git diff --check` passed; regression proves non-zero chip hook public error redacts secret-shaped stderr and local paths while preserving exit code | Await owner/security review. This is a chip hook public error-boundary lane, separate from #50 research citations and #52 missing-config guidance. Proof reply: https://github.com/vibeforge1111/spark-researcher/pull/53#issuecomment-4577855575 |
| [#441](https://github.com/vibeforge1111/spark-cli/pull/441) | `spark-cli` | Open, ready for review, mergeable; GitHub shows blocked merge state | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Update repair tests -> 3 passed; `py_compile` passed; empty-home and installed-home smokes confirmed routing | Watch for maintainer feedback. This is a clean repair-guidance lane with no validator warnings. |
| [#451](https://github.com/vibeforge1111/spark-cli/pull/451) | `spark-cli` | Open, ready for owner/security review, mergeable clean, no comments; branch pushed with follow-up commit `b8e88c2` and PR body updated after GitHub auth was restored | Valid with owner-review warning on updated packet: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | Bundle-aware fix/verify tests -> 4 passed; broader repair/verify lane -> 19 passed; `py_compile` passed; branch-local `fix telegram --json` and `verify --onboarding --json` smokes show `telegram-voice-starter` repair commands | Await owner/security review. This extends the existing bundle-aware repair-output lane to `spark fix telegram` instead of opening a duplicate PR. |
| [#452](https://github.com/vibeforge1111/spark-cli/pull/452) | `spark-cli` | Open, mergeable clean; maintainer requested security-safe redesign proof; pushed test-only boundary proof `0a082f9`; PR body now starts with packet; proof reply posted | Valid with owner-review warning after security-boundary repair: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `security_owner_review_expected` warning | Focused parser boundary tests -> 3 passed; browser-use suite with non-linked `TMPDIR` -> 23 passed; `py_compile` passed; `git diff --check` passed; new test proves option-like goal text remains available after `--` | Await owner/security review. Latest reply: https://github.com/vibeforge1111/spark-cli/pull/452#issuecomment-4578311534 |
| [#453](https://github.com/vibeforge1111/spark-cli/pull/453) | `spark-cli` | Open, maintainer reported Mac Lab `lab_failed` after packet repair; pushed focused test-only rework `28e8c4e`; PR body packet/proof updated; post-lab reply posted | Valid after post-lab body update: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | `PYTHONPATH=src python -m unittest tests.test_browser_use_cli` -> 22 passed; focused JSON redaction tests -> 2 passed; `py_compile` passed; `git diff --check` passed | Await security/reviewer re-check and another Mac Lab run. Rework keeps runtime redaction code unchanged and fixes browser-use test fixtures to use resolved process-isolated temp roots so macOS `/var` symlinked temp paths do not trip Spark CLI linked-path write protection. Reply: https://github.com/vibeforge1111/spark-cli/pull/453#issuecomment-4578056685 |
| [#440](https://github.com/vibeforge1111/spark-cli/pull/440) | `spark-cli` | Open, maintainer requested execution-shaped-path boundary proof; branch pushed with follow-up commit `13ab6ec`; proof reply posted | Valid after follow-up: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Summary tests -> 7 passed, 567 deselected; broader summary/fix/healthcheck lane -> 8 passed, 566 deselected; `py_compile` passed; `git diff --check` passed | Await owner/security review. Proof reply: https://github.com/vibeforge1111/spark-cli/pull/440#issuecomment-4577190618 |
| [#439](https://github.com/vibeforge1111/spark-cli/pull/439) | `spark-cli` | Open, ready for review, mergeable; GitHub shows blocked merge state | Valid: `packet_valid=true`, `can_continue_to_review=true`; validator warning `security_owner_review_expected` for sign-in readiness gate | Targeted provider-readiness tests -> 10 passed; `py_compile` passed; isolated onboarding smoke shows after-fix `llm_roles` failure | Watch for security/lab or maintainer feedback. Keep responses focused on redaction, sign-in readiness, and reproducible smoke. |
| [#438](https://github.com/vibeforge1111/spark-cli/pull/438) | `spark-cli` | Open, mergeable; Mac Lab failure comment answered with test-only fixture repair; GitHub now reports branch behind | Valid: `packet_valid=true`, `can_continue_to_review=true`, 0 errors/warnings | Focused live/external-ingress tests -> 5 passed after repair; previous full `tests/test_cli.py` -> 572 passed, 1 skipped | Await lab rerun/reviewer response. Do not update branch unless the reviewer asks or the behind state becomes the blocker. |
| [#278](https://github.com/vibeforge1111/spark-telegram-bot/pull/278) | `spark-telegram-bot` | Open, ready for review, mergeable clean; maintainer requested bounded proof/warning clarification; PR body clarified and proof reply posted | Valid with expected Telegram no-safe-chat warning: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `telegram_proof_unavailable` warning | Direct Builder bridge test file -> passed; `npm run build` -> passed; `git diff --check` passed; repo runner continued into unrelated buildE2E self-awareness failure; npm audit reports existing axios lane | Await reviewer/lab. Patch makes Telegram browser-proof receipt extraction choose the newest matching `spark_browser` receipt by `created_at`, so stale success no longer overrides newer failure. Latest reply: https://github.com/vibeforge1111/spark-telegram-bot/pull/278#issuecomment-4578265646 |
| [#279](https://github.com/vibeforge1111/spark-telegram-bot/pull/279) | `spark-telegram-bot` | Open, ready for review, mergeable clean; maintainer asked for reviewer-verifiable onboarding/rate-limit proof; follow-up commit `b6448b2` pushed and proof reply posted | Valid with expected Telegram no-safe-chat warning: `packet_valid=true`, `can_continue_to_review=true`, 0 errors, 1 `telegram_proof_unavailable` warning | `npm run build` -> passed; `BOT_TOKEN=123:test node node_modules/ts-node/dist/bin.js tests/spawnerLoopBugHunt.test.ts` -> passed; `git diff --check` -> passed; regression now fills a user window, blocks a fourth normal/action command, and proves `/myid` plus `/start session-123` still pass | Await reviewer/lab. Patch keeps `/start` and `/myid` reachable before the global burst limiter, while non-onboarding commands still rate-limit. Duplicate note: #137 covers generic non-onboarding rate-limit notices; #279 is the narrower first-run onboarding reachability lane. Latest reply: https://github.com/vibeforge1111/spark-telegram-bot/pull/279#issuecomment-4578513791 |
| [#224](https://github.com/vibeforge1111/spark-telegram-bot/pull/224) | `spark-telegram-bot` | Open; maintainer requested reshape/split; reply posted | Older packet status not used as next lane | Maintainer/lab previously reported Mac Lab passed; upstream now has patch-equivalent commit `014f17f` | Do not force-push unless reviewer asks. Treat as adopted/overtaken and keep the public note concise. |

## Closed Or Overtaken PRs

| PR | Repo | Result | Evidence | Follow-up |
| --- | --- | --- | --- | --- |
| [#419](https://github.com/vibeforge1111/spark-cli/pull/419) | `spark-cli` | Closed as overtaken/adopted after maintainer asked for a rebase and current-pin reduction | Current `origin/master` at `b07d72d` already carries the current registry pins after merged registry path #421 (`f313910`); `verify --registry-pins --json` and `verify --provenance --json` both returned `ok: true` on a clean current-master checkout | Close comment posted: https://github.com/vibeforge1111/spark-cli/pull/419#issuecomment-4577503586 |

## Ready Replies

### Packet Or Format Issue

Thanks, fixed. I repaired the packet in the PR body and re-ran the Spark Compete validator. Current result: `packet_valid=true`, `can_continue_to_review=true`, with 0 errors and 0 warnings.

### Tests Or Repro Question

Thanks, confirmed. The before state reproduces with the command listed in the packet, and the current branch verifies with the listed test command. I kept the patch scoped to the files named in the packet.

### Split Or Scope Concern

Thanks, agreed. I will keep this to one root cause and one owner surface. If this PR is too broad, I can close it and open a smaller focused replacement rather than force-pushing unrelated changes.

### Overtaken Or Duplicate

Thanks, confirmed. I checked current upstream before changing the branch. If the fix is already patch-equivalent upstream, I will not force-push noise; please treat this as adopted/overtaken or tell me if you prefer a clean replacement PR.

## Next Work Gate

Open another PR only when all are true:

- It is in an allowed public repo.
- The issue is reproducible without private evidence.
- The patch is one root cause and one owner surface.
- Tests pass locally.
- The PR body includes a validator-clean `spark-compete-hotfix-v1` packet.
- Existing review comments are already answered.

## Candidate Queue

| Candidate | Repo | Why it may be worth checking | Gate before work |
| --- | --- | --- | --- |
| Research URL/evidence handling | `spark-researcher` | PR #50 covers unsafe/non-public DuckDuckGo result URLs being preserved as research prompt sources. | Avoid duplicating citation URL hygiene; inspect a separate researcher surface only if it has a distinct root cause. |
| Agent-site legal footer route | `Spark-Agent-Site` | PR #63 covers stale legal-page GitHub links to retired `spark-skill-graphs`. | Avoid duplicating legal/footer route work; inspect another site surface only if it has a separate root cause. |
| Spawner UI dependency/runtime baseline | `vibeship-spawner-ui` | PR #133 covers the fresh-install high/moderate npm audit baseline with a lockfile-only compatible refresh. | Avoid dependency-audit duplicates; inspect Spawner UI only for separate Mission Control, preview, provider-result, or UI behavior failures. |
| Spark CLI onboarding verify guidance | `spark-cli` | PR #451 covers configured-bundle repair commands for `spark verify --onboarding`; #438-#441 cover separate CLI repair/readiness lanes. | Avoid more CLI guidance PRs unless the failure is clearly separate from bundle-aware verify output, repair summaries, provider readiness, live ingress, or update repair. |
| Voice install/onboard edge cases | `spark-voice-comms` | PR #19 covers Kokoro Python support, PR #20 covers invalid hook JSON shape, and PR #21 covers malformed `voice.transcribe` audio base64. | Avoid those three lanes; inspect a separate runtime/provider or docs path only if it has a distinct root cause. |
| Telegram rate-limit user notice | `spark-telegram-bot` | Generic notice behavior already has open PR #137; #279 now covers the separate `/start` and `/myid` onboarding reachability gap. | Do not open more rate-limit lanes unless reviewer asks or a distinct non-overlapping root cause appears. |
| Builder attachment hook execution | `spark-intelligence-builder` | PR #106 covers process-0/output-returncode-1 misclassification. | Avoid duplicating hook returncode; inspect other builder surfaces only if separate root cause. |
| Character live-search source handling | `spark-character` | PR #23 covers DuckDuckGo redirect URL decoding; PR #24 covers live-search snippet prompt-boundary sanitization. | Avoid duplicating those two lanes; inspect a separate character surface only if it has a distinct root cause. |
| Personality hook host-output contract | `spark-personality-chip-labs` | PR #17 covers missing/non-object input in `spark_hook.py`; adjacent hook boundaries may still be worth checking. | Avoid overlapping PR #13 unsupported hook and PR #16 `hooks.py` stdin handling. |
| Personality validator CLI | `spark-personality-chip-labs` | PR #18 covers empty directories, template skipping, and current bridge summary keys. | Avoid overlapping directory validation behavior; look elsewhere unless reviewers ask. |
| Domain-chip local test collection | `spark-domain-chip-labs` | PR #24 covers optional `jsonschema` collection failures; PR #25 covers the Startup YC saved fixture hash mismatch. | Avoid duplicating importorskip or saved-hash work; move to another domain-chip lane only if it has a separate root cause. |
